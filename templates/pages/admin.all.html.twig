{% extends 'base.html.twig' %}
{% block title %}All{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <button class="btn btn-primary"><a class="text-decoration-none text-center text-white" href="{{ path('admin.movie.create') }}">Ajouter un film</a></button>
            </div>
        </div>
    </div>
    <h1>{{number}}</h1>
{% endblock %}