<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="/css/bootstrap.css">
        <title>{% block title %}Welcome!{% endblock %}</title>

        {% block stylesheets %}{% endblock %}
    </head>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand {% if current_view != "home" %}text-muted{% endif %}" href="{{ path('home') }}">Home</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link {%if current_view == "films"%}active{% endif %}" href="{{ path('films') }}">Films</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {%if current_view == "series"%}active{% endif %}" href="{{ path('series') }}">Séries</a>
                    </li>
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Logout</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {%if current_view == "tag"%}active{% endif %}" href="{{ path('tag') }}">Tag</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
